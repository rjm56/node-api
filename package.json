{
  "name": "todolistapi",
  "version": "1.0.0",
  "description": "API set up",
  "main": "index.js",
  "scripts": {
    "test": "\"echo \\\"Error: no tests specified\\\" && exit 1\"",
    "start": "yarn build && nodemon dist/server",
    "build": "rimraf dist/ && babel ./api --out-dir dist/ --copy-files"
  },
  "repository": {
    "type": "git",
    "url": "\"https://github.com/rjm56/node-api.git\""
  },
  "author": "Rosie",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^1.17.2",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "mongoose": "^5.0.10"
  }
}